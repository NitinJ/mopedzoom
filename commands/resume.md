---
description: Resume a paused mopedzoom task
---

Ask the user for the task id. Run `mopedzoom resume <id>` and print the response.

If the task is not paused (e.g. it was cancelled or failed), explain why resume is not applicable and suggest alternatives (`/mopedzoom:submit` to re-run, or `/mopedzoom:edit` to adjust remaining stages).
